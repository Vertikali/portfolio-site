// Добавление и удаление ссылок

////////////////////////////////////////////////////////

// Добавление гиперссылки на карту ссылок
function addLink(link)
{        
    let parametr = $("<area>", link);
    $("map").prepend(parametr);
}

// Удалить ссылки
function removeLinks()
{
    $("area").remove();
}

////////////////////////////////////////////////////////